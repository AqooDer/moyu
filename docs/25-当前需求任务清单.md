# 25 - 当前需求任务清单

> 本清单只维护当前和后续仍有价值的 feature / docs / refactor 事项。已完成任务不再逐条展开，避免主文档变成历史流水账。

## 使用方式

- 领取任务前，先复制 [feature-delivery-checklist.md](./checklists/feature-delivery-checklist.md) 或直接在本清单补齐同等信息。
- 一个任务对应一个闭环。
- 完成后记录验证结果和 commit。

## 已落地能力摘要

F-001 到 F-011 已完成并合并为当前架构事实，详情以 git history 为准。已落地能力包括：

- Workbench 设置中心独立数据入口与 hash 路由
- 模型角色配置和 Agent 覆盖链路
- Workspace 知识库配置读取
- Artifact -> Knowledge Base write-back 记录与策略校验
- Skill / Tool / MCP 设置展示
- Agent 安装冲突版本化处理
- Work / Message 本地持久化 store
- Workbench 正式前端目录
- Agent Runtime Context 设置视图
- `src/settings/` 领域目录拆分
- 本地环境、生成物和忽略规则清理

## F-012 重整 docs 文档体系

- 状态：已完成
- 建议 scope：`docs`
- 背景：`docs/` 同时保留了原始需求、早期方案、ADR、规格、验收记录、工程状态和已完成 checklist，读者难以判断哪些是当前事实。
- 目标：把文档体系收敛成面向当前仓库事实的架构与运行文档，删除已经被新文档覆盖的历史草稿和过期交付记录。
- 本次范围：
  - 重建 `docs/README.md` 作为文档入口
  - 重写核心概念、总体架构、Agent / Meta-Agent、运行时、Workbench、工程状态、协作规范与当前清单
  - 整合 Trace、Artifact、Agent 文件夹、UI Schema、Skill 生成、旧路线图和验收记录
  - 删除被整合后的过期文档和已完成 checklist，保留 checklist 模板
- 非本次范围：
  - 不改运行时代码、前端代码或 Agent 实现
  - 不新增真实功能
  - 不补做 `file://` 人工验收
- 验收标准：
  - `docs/README.md` 能说明当前文档体系、阅读路径和历史文档处理原则
  - 核心架构文档与 `src/`、`agents/`、`ui/workbench/` 当前结构一致
  - 工程运行文档命令与 `package.json` 一致
  - 当前需求和缺陷清单只保留仍有价值的事项
  - Markdown 内部链接不存在明显断链
- 测试 / 验证：
  - `rg` 检查被删除文件无残留引用：通过
  - Markdown 相对链接检查：15 个 Markdown 文件全部通过
  - `git diff --check`：通过
- 闭环 commit：`docs(architecture): 重整 docs 文档体系`

## F-013 Workbench 前端继续组件化

- 状态：待领取
- 建议 scope：`frontend`
- 背景：`ui/workbench/src/app.js` 仍承担较多状态和渲染逻辑，后续 Settings、Artifact、Trace、安装冲突等能力继续增长会提高回归风险。
- 目标：继续拆分 Workbench 前端状态、渲染和 API 调用模块。
- 本次范围：
  - 梳理 `app.js` 中 Work、Run、Artifact、Agent、Settings、Install 相关职责
  - 优先抽出低风险纯渲染或纯数据转换函数
  - 保持当前 UI 行为不变
- 非本次范围：
  - 不引入 React / Vite 等新构建链
  - 不重做视觉设计
  - 不改变 API contract
- 验收标准：
  - 关键交互无回退：Work 切换、Agent run、Meta-Agent create、install conflict、Settings 路由
  - 前端模块边界比当前更清晰
- 测试 / 验证：
  - `npm test`
  - `npm run typecheck`
  - `npm run build`
  - 浏览器手工验收 Workbench 主路径
- 闭环 commit：

## F-014 AgentDraft 数据层版本化

- 状态：待领取
- 建议 scope：`meta`
- 背景：AgentDraft 当前以 `agent-draft.json` 和 `agent-drafts.json` 索引表达，可用但仍偏文件索引。
- 目标：让 AgentDraft 更适合查询、版本化和后续 UI 审核。
- 本次范围：
  - 明确 AgentDraft schema 演进规则
  - 增加查询和状态更新的测试覆盖
  - 为后续逐文件审核 UI 留出数据字段
- 非本次范围：
  - 不引入数据库
  - 不实现逐文件可视化合并器
- 验收标准：
  - AgentDraft 状态流更清晰
  - 安装成功、安装冲突、创建版本都有稳定记录
- 测试 / 验证：
  - Meta-Agent create / install / conflict 流程测试
- 闭环 commit：

## F-015 Settings 编辑保存能力

- 状态：待领取
- 建议 scope：`settings`
- 背景：Settings 当前主要是只读架构面板，无法从 UI 保存 Workspace 配置。
- 目标：支持安全编辑 `moyu.config.json` 中的模型角色和知识库配置。
- 本次范围：
  - 定义可编辑字段
  - 增加保存 API
  - 增加写入前校验
  - UI 覆盖保存中、成功、失败和回滚提示
- 非本次范围：
  - 不实现真实知识库索引
  - 不接入外部 Provider 健康检查
  - 不保存 API key
- 验收标准：
  - 可编辑模型角色和知识库基础配置
  - 保存结果可被 `/api/settings` 重新读取
  - 错误配置不会破坏现有默认值
- 测试 / 验证：
  - Settings API 测试
  - Workbench 手工验收
- 闭环 commit：
