# 25 - 当前需求任务清单

> 来源：`README*`、`21-工程运行与状态.md`、`23-v0.1-alpha验收记录.md`、当前原型实现状态。执行规则：**每次只领取一条，完成代码 + 测试 + 验证 + 文档 + commit 闭环后，再开始下一条。**

## 使用方式

- 领取任务前，先复制 [feature-delivery-checklist.md](./checklists/feature-delivery-checklist.md)
- 一个任务对应一个 Issue
- 一个任务对应一个 Commit
- Commit 完成后，把结果和 commit hash 回写到本清单

---

## F-001 设置中心落地为真实前端模块

- 状态：已完成
- 建议 scope：`workbench`
- 目标：把设置中心从静态原型迁移为正式前端模块，具备真实路由、状态管理和 API 数据接入
- 本次范围：
  - 增加独立设置中心 API 数据入口
  - 支持 `#settings/<section>` hash 路由直达设置分组
  - 设置中心使用独立前端状态，覆盖加载态、空态、失败态与重试
  - 保留当前 Workbench 原型结构，不引入新前端框架
- 非本次范围：
  - 不迁移到 React/Vite 等完整前端工程
  - 不实现设置项编辑保存
  - 不改变现有 Agent 运行与产物流程
- 验收标准：
  - Models / Knowledge / Skills / Tools / MCP / Runtime 六个分组可切换
  - 数据来自真实 API，不依赖页面硬编码
  - 设置页具备空态、加载态、失败态
- 测试 / 验证：
  - API 测试覆盖 `/api/settings`
  - 前端渲染逻辑测试覆盖加载 / 空 / 失败状态
  - `npm test` 通过
  - `npm run typecheck` 通过
  - `npm run build` 通过
  - `git diff --check` 通过
  - 浏览器验收通过：`#settings/models`、`#settings/knowledge`、`#settings/mcp`、`#settings/runtime` 均可直达并渲染对应设置分组
- 闭环 commit：`feat(workbench): 增加设置中心独立状态与路由`

## F-002 模型角色配置与 Agent 继承链路

- 状态：已完成
- 建议 scope：`runtime`
- 目标：把“对话模型 / 推理模型 / 知识库嵌入模型 / 生图模型”等角色从文档概念落到可执行配置
- 本次范围：
  - 增加 Workspace 默认模型角色配置读取
  - 支持 Agent Manifest 局部覆盖模型角色
  - 在生图 Agent Run Trace 中记录最终命中的模型角色、Provider、模型与回退原因
- 非本次范围：
  - 不实现复杂模型路由器
  - 不实现设置页编辑表单
  - 不接入真实知识库索引或多 Provider 健康检查
- 验收标准：
  - Workspace 默认角色可配置
  - Agent 可继承默认值并局部覆盖
  - Run 可记录最终命中的模型角色、Provider 与回退原因
- 测试 / 验证：
  - 配置读取测试：`src/settings/models/model-roles.test.ts`
  - Run Trace 断言：`src/runtime/workbench-data.test.ts`
  - `npm test` 通过
  - `npm run typecheck` 通过
  - `npm run build` 通过
- 闭环 commit：`feat(runtime): 增加模型角色配置与运行记录`

## F-003 Workspace 知识库基础层

- 状态：已完成
- 建议 scope：`knowledge`
- 目标：提供知识库集合、切片策略、嵌入模型、连接 Agent、产物回流规则的最小闭环
- 本次范围：
  - 增加 Workspace 知识库配置读取层，支持 `moyu.config.json` 声明知识库集合
  - 支持集合级嵌入角色、切片策略、来源、连接 Agent 与允许回流产物类型
  - Workbench `/api/settings` 与 Settings → Knowledge 展示真实配置数据
  - 更新配置示例、工程状态文档与交付清单
- 非本次范围：
  - 不实现真实文档切片、向量索引、召回查询
  - 不实现 Agent 产物写回动作或审核 UI
  - 不引入 SQLite / LanceDB / Worker 等持久化执行层
- 验收标准：
  - 可声明一个 Workspace 知识库集合
  - 可绑定嵌入角色与切片策略
  - Agent 可选择使用哪些知识库
  - 可配置“哪些产物允许回流知识库”
- 测试 / 验证：
  - 知识库配置读取测试
  - Workbench Settings API 断言配置透出
  - Settings → Knowledge 手工联调
  - `npm test` 通过
  - `npm run typecheck` 通过
  - `npm run build` 通过
  - `git diff --check` 通过
- 闭环 commit：`feat(knowledge): 增加 Workspace 知识库配置层`

## F-004 Agent 产物回流知识库

- 状态：已完成
- 建议 scope：`artifact`
- 目标：把被采纳的文档、摘要、图像描述等产物加入知识库，形成可追踪闭环
- 本次范围：
  - 增加 Artifact → Knowledge Base write-back 运行时记录
  - 支持 CLI 标记已审核产物加入目标知识库集合
  - 写回记录包含来源 Artifact、目标集合、审核人、审核说明与时间
  - Run Trace 与 `run show` 可看到 write-back 事件
  - 接入 F-003 `knowledge_bases` 配置，校验集合存在、回流启用和允许产物类型
- 非本次范围：
  - 不实现真实切片、嵌入、向量索引或召回
  - 不实现设置中心审核 UI 或后台 Worker
- 验收标准：
  - 产物可标记“加入知识库”
  - 写回动作有来源、目标集合、审核记录
  - Trace 中可看到 write-back 事件
- 测试 / 验证：
  - 产物写回流程测试
  - 回流记录查询验证
  - 知识库回流策略校验测试
  - `npm test` 等价命令通过
  - `npm run typecheck` 通过
  - `npm run build` 通过
  - `git diff --check` 通过
- 闭环 commit：`3cf898a feat(artifact): 增加产物知识库回流记录`；`f2f4cfb feat(artifact): 接入知识库回流策略校验`

## F-005 Skill / Tool / MCP 基础设置与权限边界

- 状态：待开始
- 建议 scope：`settings`
- 目标：为 Skill、Tool、MCP 补齐基础注册、启停、作用范围和审核状态呈现
- 验收标准：
  - Settings 中可查看 Skill / Tool / MCP 列表
  - 每项可看到来源、状态、作用范围
  - 受控生成 Skill 与普通内置 Skill 有清晰区分
- 测试 / 验证：
  - API 数据模型测试
  - 设置页渲染测试
- 闭环 commit：

## F-006 安装冲突的版本化处理路径

- 状态：待开始
- 建议 scope：`meta`
- 目标：把“install_conflict” 从提示提升到有明确下一步的版本化流程
- 验收标准：
  - 能看到冲突 Agent、源路径、目标路径
  - 提供“创建新版本”或“查看差异”的明确入口
  - 不允许静默覆盖
- 测试 / 验证：
  - API 冲突流程测试
  - UI 点击验收
- 闭环 commit：

## F-007 Work / Message 持久化数据层

- 状态：待开始
- 建议 scope：`runtime`
- 目标：把 Work / Message 从原型状态推进为正式可查询的数据层
- 验收标准：
  - Work、Message 有独立持久化结构
  - Workbench 左侧列表与中间会话来自真实存储
  - 可支持后续搜索、筛选和状态恢复
- 测试 / 验证：
  - 数据层测试
  - API 查询测试
- 闭环 commit：

## F-008 Workbench 原型迁移到正式前端工程

- 状态：待开始
- 建议 scope：`frontend`
- 目标：把 `ui/workbench-prototype/` 收敛成正式前端结构，复用现有 API
- 验收标准：
  - 具备独立工程结构、组件划分、状态管理
  - 原型当前功能无明显回退
  - 新设置中心被纳入正式工程
- 测试 / 验证：
  - 构建与路由验证
  - 关键交互回归
- 闭环 commit：

## F-009 参考 Yuxi 补齐 Agent 运行上下文装配视图

- 状态：已完成
- 建议 scope：`workbench`
- 背景：Yuxi 把模型、知识库、Skills、MCP、工具、子智能体、沙盒与中间件作为 Harness 能力统一装配。Moyu 不应照搬企业级重底座，但需要在设置中心清晰展示 Agent 运行时会继承和装配哪些能力。
- 目标：在 Workbench 设置中心增加 Agent Runtime Context 视图，把模型角色、知识库、Skill、Tool、MCP、运行证据和产物策略按 Agent 维度串起来。
- 本次范围：
  - 增加原型 API 数据结构
  - 增加设置中心分组与前端渲染
  - 补测试断言
- 非本次范围：
  - 不实现真实知识库索引
  - 不实现 MCP 服务启停
  - 不实现 Skill 安装/审核后端
- 验收标准：
  - Settings 中出现 Agent Context 分组
  - 可看到 Meta-Agent 与生图 Agent 的运行上下文装配
  - API / 原型测试覆盖该数据结构
- 测试 / 验证：
  - `npm test` 通过
  - `npm run typecheck` 通过
  - `npm run build` 通过
  - 浏览器验收通过：Settings → Agent Context 可看到 Meta-Agent 与生图 Agent 的上下文装配
- 闭环 commit：`feat(workbench): 增加 Agent 运行上下文装配视图`

## F-010 拆分设置领域源码目录

- 状态：已完成
- 建议 scope：`structure`
- 背景：`src/runtime/workbench-data.ts` 当前混放 Run/Trace/Artifact 数据聚合与 Settings 中的模型、知识库、Skill、Tool、MCP 配置数据，长期会让 runtime 职责过宽。
- 目标：建立 `src/settings/` 领域目录，把模型、知识库、Skill、Tool、MCP 等设置能力从 runtime 中拆出，形成后续配置管理和权限边界的源码基础。
- 本次范围：
  - 新增 `src/settings/` 及 `models`、`knowledge`、`skills`、`tools`、`mcp` 子目录
  - 迁移模型角色配置读取到 `settings/models`
  - 迁移知识库配置读取到 `settings/knowledge`
  - 迁移 Workbench Settings 类型与构建逻辑到 `settings`
  - 更新导入路径、测试路径和架构文档中的当前目录说明
- 非本次范围：
  - 不删除仓库顶层无用文件或目录
  - 不实现设置项编辑保存
  - 不实现真实 MCP 服务启停、Skill 注册执行、知识库向量索引
  - 不改变现有 API 返回结构和运行行为
- 验收标准：
  - `src/runtime/` 不再承载模型角色和知识库设置读取代码
  - `src/settings/models`、`src/settings/knowledge`、`src/settings/skills`、`src/settings/tools`、`src/settings/mcp` 均有明确职责文件
  - `/api/settings` 和 Workbench 数据结构保持兼容
- 测试 / 验证：
  - `npm test` 通过
  - `npm run typecheck` 通过
  - `npm run build` 通过
  - `git diff --check` 通过
- 闭环 commit：`refactor(structure): 拆分设置领域源码目录`

## F-011 清理仓库本地环境与生成物目录

- 状态：已完成
- 建议 scope：`structure`
- 背景：当前仓库存在 IDE 配置、项目内通用 Codex skill、构建输出、运行 Trace 与 Artifact 等本地环境/生成物痕迹，容易干扰源码目录判断。
- 目标：把确定不属于 Moyu 源码的本地环境文件从版本库移除，并补齐忽略规则；清理低风险本地生成目录。
- 本次范围：
  - 从版本库移除 `.idea/` IDE 配置
  - 从版本库移除 `.codex/skills/engineering-workflow`，通用 skill 保持安装在全局 skillhub / Codex / Claude 环境
  - 补充 `.gitignore`，避免 IDE、skill 安装目录、日志、覆盖率、环境变量变体再次入库
  - 删除本地 `dist/`、`artifacts/`、`traces/` 生成目录
  - 更新工程运行文档中的本地目录约定
- 非本次范围：
  - 不删除 `agents/custom__image-prototype-v1/v2/v3`
  - 不删除或重命名 `brand/logo/img*.png` 候选素材
  - 不迁移 Workbench 原型目录
  - 不删除 `node_modules/`，避免后续验证与开发需要重新安装依赖
- 验收标准：
  - `.idea/` 和 `.codex/skills/` 不再被 Git 跟踪
  - 本地生成目录清理后不会进入 Git 状态
  - `git status --short` 只剩本次预期清理改动
  - 测试、类型检查、构建和 diff 检查通过
- 测试 / 验证：
  - `npm test` 通过
  - `npm run typecheck` 通过
  - `npm run build` 通过
  - `git diff --check` 通过
- 闭环 commit：`chore(structure): 清理本地环境与生成物目录`
