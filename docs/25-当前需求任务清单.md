# 25 - 当前需求任务清单

> 本清单只维护当前和后续仍有价值的 feature / docs / refactor 事项。已完成任务不再逐条展开，避免主文档变成历史流水账。

## 使用方式

- 领取任务前，先复制 [feature-delivery-checklist.md](./checklists/feature-delivery-checklist.md) 或直接在本清单补齐同等信息。
- 一个任务对应一个闭环。
- 完成后记录验证结果和 commit。
- 后续新任务必须绑定 [27 - 目标架构与大节点任务规划](./27-目标架构与大节点任务规划.md) 中的 P0-P6 节点。
- 如果任务不属于 P0-P6，先更新路线或补充架构决策，再领取实现。

## 路线执行顺序

当前目标路线以 `27` 为准。推荐执行顺序：

1. **P0 架构基线与任务系统**
   固化路线、任务清单、DoD、架构决策记录和阶段 demo 标准。
2. **P1 框架层完善**
   先把 Host/Harness 做稳：对话编排、Work 管理、运行时、沙盒文件系统、产物、插件、权限、中间件、异步 Worker 和 Trace。
3. **P2 Meta-Agent 生成可运行 Agent**
   Meta-Agent 生成 Agent contract、UI、Skill/Tool/MCP 装配、测试和沙盒 dry-run，安装前可审核。
4. **P3 Agent 对话机制**
   业务 Agent 支持 plan-first、追问、插话、流式执行、多产物交付、失败恢复、继续任务和 SubAgent handoff。
5. **P4 知识与文件能力**
   支持 PDF、DOC/DOCX、Office、HTML、表格、知识入库、RAG middleware 和文件权限策略。
6. **P5 插件生态**
   统一插件 manifest、安装审核、运行权限、本地开发、远程源、市场 UI 和审计。
7. **P6 产品化与多入口**
   桌面壳、多入口一致性、配置中心、数据迁移、备份导出、本地安全和更新机制。

## 阶段任务池

本节列后续可领取任务，也保留本次已覆盖的 P0 落点方便对齐。完整节点拆分见 `27`。

### P0 - 架构基线与任务系统

- 已完成：P0.1 固化路线基线，由 `F-016` 覆盖。
- 已完成：P0.2 阶段型任务清单改造，由 `F-016` 覆盖。
- `F-017` P0.3 节点 DoD：定义每个 P 节点交付必须包含的文档、代码、测试、验证和演示路径。
- `F-018` P0.4 架构决策记录机制：确定 ADR 目录、模板和触发条件。
- `F-019` P0.5 阶段最小 demo 标准：定义每个阶段可向用户演示的最小闭环。

### P1 - 框架层完善

- `F-020` P1.1 Conversation Orchestrator 规格：定义 Plan、插话、追问、确认点、恢复和对话状态机。
- `F-021` P1.2 Work Manager 规格：统一 Work / Plan / Run / Step 生命周期和状态恢复。
- `F-022` P1.3 Execution Runtime 规格：定义通用 step runner 和 tool / skill / mcp / agent_call / user_checkpoint 执行契约。
- `F-023` P1.4 Sandbox Filesystem 规格：定义 workspace / uploads / outputs / temp、SandboxFile 和安全边界。
- `F-024` P1.5 Artifact Service 规格：定义产物登记、预览元数据、版本、diff、下载和交付。
- `F-025` P1.6 Previewer 插件 v1：先覆盖 md、txt、json、图片、pdf、doc/docx。
- `F-026` P1.7 Plugin Registry 规格：统一 Skill / Tool / MCP / Previewer / Middleware 注册、启停和权限声明。
- `F-027` P1.8 Permission & Policy 规格：覆盖文件、网络、命令、插件安装、MCP 和子智能体调用审批。
- `F-028` P1.9 Middleware Pipeline 规格：附件解析、历史摘要、RAG 注入、动态工具注入。
- `F-029` P1.10 Async Worker 规格：长任务后台执行、取消、重试、流式事件和恢复。
- `F-030` P1.11 Observability 规格：TraceEvent、debug log、成本/耗时统计和回放入口。

### P2 - Meta-Agent 生成可运行 Agent

- `F-031` P2.1 Agent Contract v1：manifest、ui、skills、tools、mcp、middlewares、policies、tests。
- `F-032` P2.2 需求澄清对话：Meta-Agent 追问目标、输入、输出、权限、工具和验收标准。
- `F-033` P2.3 Agent 规划器：选择 Agent 类型、执行模式、插件和 UI。
- `F-034` P2.4 Skill 生成与审核：分级生成 LLM Skill、配置型 Skill 和代码型 Skill。
- `F-035` P2.5 Tool / MCP 装配：从 registry 选择能力，不足时提出安装或生成请求。
- `F-036` P2.6 UI Schema 生成：生成输入表单、运行状态、产物视图和设置项。
- `F-037` P2.7 测试用例生成：自动生成 dry-run case 和最小验收用例。
- `F-038` P2.8 Sandbox dry-run：安装前在沙盒内跑通 demo。
- `F-039` P2.9 安装审核：文件 diff、权限 diff、测试结果、风险说明和版本化安装。
- `F-040` P2.10 Agent 迭代：根据失败、反馈和 Trace 自动提出补丁。

### P3 - Agent 对话机制

- `F-041` P3.1 Agent Chat Session：独立会话、记忆边界和上下文压缩策略。
- `F-042` P3.2 Plan-first 交互：复杂任务先给计划，用户可确认、修改或跳过。
- `F-043` P3.3 Ask-user checkpoint：信息不足时中断追问。
- `F-044` P3.4 Streaming execution：实时展示步骤、工具调用、产物生成和错误恢复。
- `F-045` P3.5 Multi-artifact delivery：多产物交付并标记 primary / intermediate / report。
- `F-046` P3.6 Artifact edit feedback：产物编辑和采纳回写为反馈。
- `F-047` P3.7 Continue / resume：跨天继续同一 Work。
- `F-048` P3.8 Failure recovery：失败时支持从指定 step 重跑。
- `F-049` P3.9 SubAgent handoff：主 Agent 调度子 Agent 并回传结果。
- `F-050` P3.10 Conversation quality eval：记录满意度、人工修改率、重跑率和失败原因。

### P4 - 知识与文件能力

- `F-051` P4.1 文件解析接口：统一 parser contract。
- `F-052` P4.2 PDF parser：文本、表格、图片、页码和引用定位。
- `F-053` P4.3 DOC/DOCX parser：结构、标题、段落、表格和批注。
- `F-054` P4.4 Office exporter：docx / pptx / xlsx 生成与预览。
- `F-055` P4.5 HTML preview sandbox：安全预览 HTML 产物。
- `F-056` P4.6 Knowledge ingestion：解析结果进入知识库待审核队列。
- `F-057` P4.7 RAG middleware：按需检索并带引用回答。
- `F-058` P4.8 Knowledge write-back：产物采纳后进入知识库。
- `F-059` P4.9 文件权限策略：上传、生成、下载、删除和外部打开纳入 policy。

### P5 - 插件生态

- `F-060` P5.1 Plugin manifest：统一描述 Skill / Tool / MCP / Previewer / Middleware。
- `F-061` P5.2 插件安装流程：解析草稿、权限声明、静态检查和确认安装。
- `F-062` P5.3 插件运行权限：按 Agent / Workspace / Work 授权。
- `F-063` P5.4 本地插件开发模式：热加载、测试和打包。
- `F-064` P5.5 远程插件源：签名、版本和信任边界。
- `F-065` P5.6 插件市场 UI：浏览、安装、升级、禁用和卸载。
- `F-066` P5.7 插件审计：运行记录、错误、成本和权限使用。

### P6 - 产品化与多入口

- `F-067` P6.1 桌面壳选型：Electron / Tauri / Web-only 决策。
- `F-068` P6.2 多入口一致性：CLI、Workbench、Desktop 共享 Framework Host。
- `F-069` P6.3 配置中心：模型、Provider、知识库、插件、权限和沙盒策略可编辑。
- `F-070` P6.4 数据迁移：JSON store -> SQLite 或其他本地持久化。
- `F-071` P6.5 备份与导出：Agent、Work、Artifact、Trace 和配置打包。
- `F-072` P6.6 本地安全：Keychain、敏感文件保护和审计日志。
- `F-073` P6.7 更新机制：版本升级、迁移脚本和插件兼容性检查。

## 旧待办路线归属

- `F-013 Workbench 前端继续组件化`：归属 P6.2 / P6.3，等 Framework Host 和配置中心规格清晰后再领取。
- `F-014 AgentDraft 数据层版本化`：归属 P2.9 / P2.10，可并入 Agent Contract 和安装审核主线。
- `F-015 Settings 编辑保存能力`：归属 P6.3，在权限、插件和沙盒策略字段明确后再实现。

## 已落地能力摘要

F-001 到 F-011 已完成并合并为当前架构事实，详情以 git history 为准。已落地能力包括：

- Workbench 设置中心独立数据入口与 hash 路由
- 模型角色配置和 Agent 覆盖链路
- Workspace 知识库配置读取
- Artifact -> Knowledge Base write-back 记录与策略校验
- Skill / Tool / MCP 设置展示
- Agent 安装冲突版本化处理
- Work / Message 本地持久化 store
- Workbench 正式前端目录
- Agent Runtime Context 设置视图
- `src/settings/` 领域目录拆分
- 本地环境、生成物和忽略规则清理

## F-012 重整 docs 文档体系

- 状态：已完成
- 建议 scope：`docs`
- 背景：`docs/` 同时保留了原始需求、早期方案、ADR、规格、验收记录、工程状态和已完成 checklist，读者难以判断哪些是当前事实。
- 目标：把文档体系收敛成面向当前仓库事实的架构与运行文档，删除已经被新文档覆盖的历史草稿和过期交付记录。
- 本次范围：
  - 重建 `docs/README.md` 作为文档入口
  - 重写核心概念、总体架构、Agent / Meta-Agent、运行时、Workbench、工程状态、协作规范与当前清单
  - 整合 Trace、Artifact、Agent 文件夹、UI Schema、Skill 生成、旧路线图和验收记录
  - 删除被整合后的过期文档和已完成 checklist，保留 checklist 模板
- 非本次范围：
  - 不改运行时代码、前端代码或 Agent 实现
  - 不新增真实功能
  - 不补做 `file://` 人工验收
- 验收标准：
  - `docs/README.md` 能说明当前文档体系、阅读路径和历史文档处理原则
  - 核心架构文档与 `src/`、`agents/`、`ui/workbench/` 当前结构一致
  - 工程运行文档命令与 `package.json` 一致
  - 当前需求和缺陷清单只保留仍有价值的事项
  - Markdown 内部链接不存在明显断链
- 测试 / 验证：
  - `rg` 检查被删除文件无残留引用：通过
  - Markdown 相对链接检查：15 个 Markdown 文件全部通过
  - `git diff --check`：通过
- 闭环 commit：`docs(architecture): 重整 docs 文档体系`

## F-013 Workbench 前端继续组件化

- 状态：暂缓，已归属 P6.2 / P6.3
- 建议 scope：`frontend`
- 背景：`ui/workbench/src/app.js` 仍承担较多状态和渲染逻辑，后续 Settings、Artifact、Trace、安装冲突等能力继续增长会提高回归风险。
- 目标：继续拆分 Workbench 前端状态、渲染和 API 调用模块。
- 本次范围：
  - 梳理 `app.js` 中 Work、Run、Artifact、Agent、Settings、Install 相关职责
  - 优先抽出低风险纯渲染或纯数据转换函数
  - 保持当前 UI 行为不变
- 非本次范围：
  - 不引入 React / Vite 等新构建链
  - 不重做视觉设计
  - 不改变 API contract
- 验收标准：
  - 关键交互无回退：Work 切换、Agent run、Meta-Agent create、install conflict、Settings 路由
  - 前端模块边界比当前更清晰
- 测试 / 验证：
  - `npm test`
  - `npm run typecheck`
  - `npm run build`
  - 浏览器手工验收 Workbench 主路径
- 闭环 commit：`feat(runtime): 增加 Middleware Pipeline 快照`

## F-014 AgentDraft 数据层版本化

- 状态：暂缓，已归属 P2.9 / P2.10
- 建议 scope：`meta`
- 背景：AgentDraft 当前以 `agent-draft.json` 和 `agent-drafts.json` 索引表达，可用但仍偏文件索引。
- 目标：让 AgentDraft 更适合查询、版本化和后续 UI 审核。
- 本次范围：
  - 明确 AgentDraft schema 演进规则
  - 增加查询和状态更新的测试覆盖
  - 为后续逐文件审核 UI 留出数据字段
- 非本次范围：
  - 不引入数据库
  - 不实现逐文件可视化合并器
- 验收标准：
  - AgentDraft 状态流更清晰
  - 安装成功、安装冲突、创建版本都有稳定记录
- 测试 / 验证：
  - Meta-Agent create / install / conflict 流程测试
- 闭环 commit：

## F-015 Settings 编辑保存能力

- 状态：暂缓，已归属 P6.3
- 建议 scope：`settings`
- 背景：Settings 当前主要是只读架构面板，无法从 UI 保存 Workspace 配置。
- 目标：支持安全编辑 `moyu.config.json` 中的模型角色和知识库配置。
- 本次范围：
  - 定义可编辑字段
  - 增加保存 API
  - 增加写入前校验
  - UI 覆盖保存中、成功、失败和回滚提示
- 非本次范围：
  - 不实现真实知识库索引
  - 不接入外部 Provider 健康检查
  - 不保存 API key
- 验收标准：
  - 可编辑模型角色和知识库基础配置
  - 保存结果可被 `/api/settings` 重新读取
  - 错误配置不会破坏现有默认值
- 测试 / 验证：
  - Settings API 测试
  - Workbench 手工验收
- 闭环 commit：`feat(meta): 强制对话式创建走真实 LLM`

## F-016 目标架构与大节点任务规划

- 状态：已完成
- 建议 scope：`docs`
- 背景：需要在不受当前实现完成度限制的前提下，重新梳理 Moyu 的目标架构、框架层职责、Meta-Agent 生成 Agent 主线和 Agent 对话主线，并参考 Claude Code、Codex、Yuxi 等优秀客户端的稳定机制。
- 目标：形成一份可执行的阶段路线图，后续项目推进严格按大节点和小任务闭环领取。
- 本次范围：
  - 明确框架层除任务规划/管理、插件接入、文件预览外还应承担的能力边界
  - 对比吸收 Yuxi、Claude Code、Codex 的架构启发
  - 规划大节点任务及每个节点下的小任务
  - 更新文档入口和当前清单
- 非本次范围：
  - 不实现代码
  - 不重写现有运行时
  - 不决定具体 UI 技术栈或桌面壳最终选型
- 验收标准：
  - 新增目标架构与路线规划文档
  - 大节点任务能覆盖框架完善、Meta-Agent 生成可运行 Agent、Agent 对话、插件/文件/沙箱/异步执行等主线
  - 每个大节点包含可领取的小任务
  - 文档内明确哪些是参考借鉴，哪些是 Moyu 自己的架构决策
- 测试 / 验证：
  - Markdown 相对链接检查：13 个 Markdown 文件全部通过
  - `git diff --check`：通过
- 闭环 commit：`docs(architecture): 增加目标架构与大节点规划`

## F-074 Agent MCP 装配 v1

- 状态：已完成
- 所属节点：P1.7 Plugin Registry / P2.1 Agent Contract v1
- 建议 scope：`agent`, `runtime`, `workbench`, `docs`
- 背景：MCP 不应只是 Settings 中的展示维度，而应该是 Agent 的一部分能力装配。后续问数 Agent、研究 Agent 等都可能通过 MCP 读取数据库、文档系统或外部工具。
- 目标：让 Agent 能在 `manifest.yaml` 中声明所需 MCP Server，并在 Agent 发现、校验、运行 Trace 和 Workbench 数据中保留装配快照。
- 本次范围：
  - 定义 `mcp_servers` manifest v1 写法
  - `listAgents` / `agent show` 读取并展示 MCP 装配
  - `validateAgentFolder` 校验 MCP 声明格式
  - Run Trace 记录当前 Agent 的 MCP 装配快照
  - Workbench `selectedRun` 和 Agent 列表暴露 MCP 装配信息
  - 同步当前概念与 Agent 文档，明确真实 MCP 调用器仍是后续任务
- 非本次范围：
  - 不启动真实 MCP Server
  - 不实现 MCP Client 调用和参数协议
  - 不接数据库或问数 Agent
  - 不实现权限审批 UI
- 验收标准：
  - Agent manifest 可声明 `mcp_servers`
  - 运行 dry-run 后 `traces/<run_id>/run.json` 包含 `run.mcpServers`
  - Workbench 数据能看到 Agent 和 selectedRun 的 MCP 装配
  - 旧 Agent 不声明 MCP 时保持兼容
- 测试 / 验证：
  - `npm test`：16 个测试全部通过
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - Workbench 静态 JSON 解析检查：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(agent): 增加 MCP 装配快照`

## F-075 Conversation Orchestrator v1 骨架

- 状态：已完成
- 所属节点：P1.1 Conversation Orchestrator / P1.2 Work Manager
- 建议 scope：`runtime`, `agent`, `meta`, `workbench`, `docs`
- 背景：当前运行时已有 Work / Run / Step / Artifact / Message，但 Plan 还只是目标概念和消息类型，尚未进入 Trace、Workbench 和 CLI。
- 目标：把“先规划、再执行、可追踪”落成最小可运行骨架，让 Plan 成为 Runtime Trace、对话消息和 Workbench 数据中的一等对象。
- 本次范围：
  - 新增 PlanRecord / PlanStepRecord 类型和 RuntimeTrace.plan
  - RuntimeStore 支持写入 Plan 和更新 Plan Step 状态
  - recordRunConversation 支持写入 plan message
  - Meta-Agent 和生图 Agent 执行入口生成静态计划
  - Workbench selectedRun、/api/runs/:id 和 CLI run show 暴露 Plan
  - 同步运行时文档和核心概念说明
- 非本次范围：
  - 不实现动态 LLM 规划器
  - 不实现计划确认、修改、跳过或暂停恢复 UI
  - 不启动真实 MCP Server，不执行 MCP call
  - 不引入完整 async worker 或通用 step runner
- 验收标准：
  - 新生成 trace 包含 `plan`
  - 旧 trace 不含 `plan` 时读取为 `plan: null`
  - Workbench 对话流按“用户请求 -> 计划 -> 总结”展示
  - Meta-Agent run 和 Agent dry-run 都能在 selectedRun 中看到 Plan
- 测试 / 验证：
  - `npm test`：16 个测试全部通过
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(runtime): 增加 Plan-first Trace 骨架`

## F-076 Work Manager 生命周期 v1

- 状态：已完成
- 所属节点：P1.2 Work Manager
- 建议 scope：`runtime`, `workbench`, `docs`
- 背景：F-075 已经让 Plan 进入 Trace 和对话流，但 Workbench 当前仍主要展示 WorkRecord 静态状态，缺少从 Run / Plan / Step 恢复出的生命周期、进度和最新运行摘要。
- 目标：建立 Work Manager v1 的只读生命周期投影，让 Work 可以从本地 Trace 恢复当前状态、最新 Run、Plan 状态和 Step 进度。
- 本次范围：
  - 新增 Work lifecycle/progress 投影类型和计算函数
  - Workbench `works` 暴露 lifecycle/progress/currentRunId
  - `/api/works` 返回带 lifecycle 的 Work summary
  - 旧 WorkStore 或旧 Trace 缺少 Plan 时保持兼容
  - 同步运行时文档和当前任务清单
- 非本次范围：
  - 不实现队列、后台 worker、取消、重试或真实恢复执行
  - 不改 WorkStore 持久化 schema
  - 不增加可写 Work 状态迁移 API
  - 不重做 Workbench 前端视觉
- 验收标准：
  - Workbench Work 列表能看到从 Run / Plan / Step 推导出的 lifecycle
  - `/api/works` 可返回同样的 lifecycle/progress 信息
  - stale WorkRecord 状态能被最新 Run 状态投影纠正
  - 旧 trace / fallback work 仍可正常展示
- 测试 / 验证：
  - `npm test`：17 个测试全部通过
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(runtime): 增加 Work Manager 生命周期投影`

## F-077 Execution Runtime step runner v1

- 状态：已完成
- 所属节点：P1.3 Execution Runtime
- 建议 scope：`runtime`, `agent`, `meta`, `docs`
- 背景：F-075/F-076 已经让 Plan 与 Work 生命周期可追踪，但 Agent 和 Meta-Agent 执行入口仍手写 `startStep` / `finishStep`，失败写入、Step 状态和 Plan Step 同步缺少统一包裹。
- 目标：建立最小通用 step runner，让现有执行入口通过统一函数执行 step，自动记录 started/ended、output summary、error 和 Plan Step 状态。
- 本次范围：
  - 新增 `runRuntimeStep` / `finishRuntimeStep` 最小执行器
  - 支持 `tool` / `skill` / `llm` 等现有 `StepKind`
  - 支持 step 返回 `succeeded` / `skipped` / `failed`
  - 异常时写入 failed Step error，并继续抛出给调用方处理
  - 接入 Meta-Agent create 和 Image Agent run 的主要执行 step
  - 同步运行时文档和当前任务清单
- 非本次范围：
  - 不实现动态 DAG / workflow engine
  - 不实现队列、后台 worker、取消、重试或暂停恢复
  - 不执行真实 MCP call
  - 不改 Trace schema
- 验收标准：
  - step runner 单元测试覆盖成功、跳过和异常失败
  - Meta-Agent 和生图 Agent 生成的 Trace 仍保持原有 Plan / Step / Artifact 语义
  - 异常 step 会写入 `error.code` / `error.message`
  - 现有 Workbench/API 流程不回退
- 测试 / 验证：
  - `npm test`：21 个测试全部通过
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(runtime): 增加 step runner 骨架`

## F-078 Failed run trace finalizer v1

- 状态：已完成
- 所属节点：P1.3 Execution Runtime
- 建议 scope：`runtime`, `agent`, `meta`, `docs`
- 背景：F-077 已经让单个 Step 的异常能写入 failed Step，但执行入口抛出异常后仍可能没有 failed Run、failed Plan、Trace 和 Work 对话摘要，Workbench 无法稳定恢复失败现场。
- 目标：建立最小失败收口能力，让已有 Agent run 和 Meta-Agent create 在异常时仍落盘 failed Trace，并把失败摘要写入 Work 会话。
- 本次范围：
  - 新增 failed run finalizer，统一记录 run failed、plan failed、error note 和 trace
  - Image Agent provider / skill 异常时写出 failed Trace 和失败对话摘要
  - Meta-Agent 创建异常时写出 failed Trace 和失败对话摘要
  - 失败后继续向调用方抛出原异常，不吞掉 CLI / API 错误
  - 补充失败路径测试和运行时文档
- 非本次范围：
  - 不实现重试、恢复、从指定 step 继续执行
  - 不实现队列、后台 worker、取消或暂停恢复
  - 不改 WorkStore / Trace schema
  - 不新增失败恢复 UI
- 验收标准：
  - provider 调用失败后 `traces/<run_id>/run.json` 存在，`run.state` / `plan.state` 为 `failed`
  - 失败 Step 保留 `error.code` / `error.message`
  - Workbench / Work Manager 能从 failed Trace 投影为 failed Work
  - Meta-Agent 创建入口的目标目录冲突等异常也能留下 failed Trace 和失败对话
- 测试 / 验证：
  - `npm test`：23 个测试全部通过
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(runtime): 增加 failed run 收口`

## F-079 Sandbox Artifact Preview v1

- 状态：已完成
- 所属节点：P1.4 Sandbox Filesystem / P1.5 Artifact Service / P1.6 Previewer 插件
- 建议 scope：`runtime`, `ui`, `workbench`, `docs`
- 背景：当前 Artifact 已能登记文件并读取部分文本，但缺少统一 preview contract、沙盒路径分类、预览能力声明和 Workbench 中稳定的预览状态。用户验收产物时仍难以判断文件能否内嵌预览、应外部打开还是只能查看元数据。
- 目标：建立一个可浏览器验收的文件/产物预览闭环，让 Artifact 在 API、Workbench 数据和右侧检查器中都带有 preview metadata，并按文本、图片、PDF、Office/二进制、不支持等类型稳定展示。
- 本次范围：
  - 定义最小 Artifact preview metadata：kind、label、inline/open/extract 能力、sandbox scope、relative path、mime
  - 新增 preview 读取接口，支持文本类内容截断预览、图片内嵌 URL、PDF/Office/二进制元数据提示
  - Workbench `artifacts` 暴露 preview metadata 和 sandbox path 信息
  - Workbench 右侧 Artifact detail 根据 preview contract 渲染文本、图片和不支持状态
  - 同步运行时、UI 文档和任务清单
- 非本次范围：
  - 不实现真实 PDF/DOCX 文本解析
  - 不执行任意 HTML 产物预览
  - 不实现上传文件、删除文件或权限审批 UI
  - 不引入数据库或外部 preview service
- 验收标准：
  - Meta-Agent 草案产物能在 Workbench 右侧以文本 preview 展示
  - 图片 Artifact 仍能内嵌展示，并带 preview metadata
  - PDF/DOCX 等二进制 Artifact 返回明确的不可文本预览状态和外部打开能力
  - API 可通过 artifact id 读取统一 preview 响应
  - 旧 artifact-content 接口保持兼容
- 测试 / 验证：
  - `npm test`：26 个测试全部通过
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(runtime): 增加 artifact preview 骨架`

## F-080 Plugin Registry & Policy v1

- 状态：已完成
- 所属节点：P1.7 Plugin Registry / P1.8 Permission & Policy
- 建议 scope：`plugins`, `settings`, `ui`, `docs`
- 背景：Settings 当前分别展示 Skill / Tool / MCP / Runtime Policy，但这些能力还不是统一的 Framework Host registry 对象。后续 Agent 生成、MCP 调用、Previewer、Middleware 和权限审批都需要同一套 capability/policy 声明作为装配依据。
- 目标：建立最小 Plugin Registry & Policy v1，把 Skill、Tool、MCP、Previewer、Middleware 和 runtime policy 统一成可查询、可展示、可测试的框架层对象。
- 本次范围：
  - 新增内置 Plugin Registry 数据模型和查询函数
  - 统一 capability 类型、来源、状态、风险、权限 ID、默认启用 Agent 和审批策略声明
  - Settings 的 Skills / Tools / MCP / Runtime Policy 由 registry/policy 派生
  - Workbench API 暴露 `/api/plugins` 和 `/api/policies`
  - Workbench Settings 增加 registry/policy summary，让浏览器能验收能力总览
  - 同步架构、运行时和 UI 文档
- 非本次范围：
  - 不实现插件安装、卸载、热加载或远程市场
  - 不执行真实 MCP call 或外部插件代码
  - 不做逐次审批 UI
  - 不改变 Agent manifest schema
- 验收标准：
  - 同一 registry 能查询 skills/tools/mcp/previewers/middlewares
  - 每个可执行/外接能力都有权限边界、审批策略和风险级别
  - Settings 仍展示原有 Skills / Tools / MCP 信息，并新增 previewer/middleware/policy summary
  - `/api/plugins` 和 `/api/policies` 可返回稳定 JSON
  - 旧 Settings API 字段保持兼容
- 测试 / 验证：
  - `npm test`：28 个测试全部通过
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(plugins): 增加 registry 与 policy 声明`

## F-081 Middleware Pipeline v1

- 状态：已完成
- 所属节点：P1.9 Middleware Pipeline / P1.7 Plugin Registry
- 建议 scope：`runtime`, `middleware`, `workbench`, `docs`
- 背景：Plugin Registry 已声明 Middleware 能力，但附件、历史、知识和插件能力如何进入 Agent 运行上下文还没有运行时快照。后续文件解析、RAG、MCP 取数和动态工具注入都需要一条可追踪的上下文装配管线。
- 目标：建立最小 Middleware Pipeline v1，让每次 Run 能记录上下文装配阶段、输入输出摘要、关联 capability/policy，并在 API 与 Workbench 中可见。
- 本次范围：
  - 新增 MiddlewarePipelineRecord / MiddlewareStageRecord 类型和 RuntimeTrace.middleware
  - RuntimeStore 支持写入 middleware pipeline 快照
  - 为 Meta-Agent 创建 Agent 和生图 Agent dry-run/运行生成静态 middleware pipeline
  - Workbench selectedRun、/api/workbench、/api/runs/:id 和 CLI run show 暴露 middleware
  - Workbench 右侧 Context tab 展示附件、历史、知识和能力注入阶段
  - 同步核心概念、运行时和 UI 文档
- 非本次范围：
  - 不实现真实 RAG 检索、文件解析、异步 worker 或动态插件执行
  - 不启动真实 MCP Server，不执行 MCP call
  - 不实现用户可编辑的 middleware 编排 UI
  - 不改变现有 Agent manifest schema
- 验收标准：
  - 新生成 trace 包含 `middleware`，旧 trace 读取时默认为 `null`
  - Meta-Agent run 和 Image Agent dry-run 都能在 selectedRun 中看到 middleware pipeline
  - Context tab 能看到 pipeline 阶段、状态、输入输出摘要、capability 和 policy
  - `/api/runs/:id` 返回 trace 中的 middleware
  - Settings 中已有 Middleware registry 展示保持兼容
- 测试 / 验证：
  - `npm test`：29 个测试全部通过
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(runtime): 增加 Middleware Pipeline 快照`

## F-082 Permission & Policy Gate v1

- 状态：已完成
- 所属节点：P1.8 Permission & Policy / P1.7 Plugin Registry / P1.9 Middleware Pipeline
- 建议 scope：`runtime`, `plugins`, `workbench`, `docs`
- 背景：Plugin Registry 已经声明 capability / permission / runtime policy，Middleware Pipeline 也能记录每次 Run 装配了哪些能力，但运行时还没有把“本次实际请求了哪些权限、是否默认允许、是否需要审核、是否存在未知权限”收口成可追踪评估。后续 MCP 取数、文件系统访问、命令执行和插件安装都需要先经过框架层 Policy Gate。
- 目标：建立最小 Permission & Policy Gate v1，让每次 Run 生成只读策略评估快照，并在 Trace、CLI、API 和 Workbench 中可见。
- 本次范围：
  - 新增 PolicyEvaluationRecord / PolicyCheckRecord 类型和 RuntimeTrace.policy
  - RuntimeStore 支持写入 policy evaluation 快照，旧 trace 读取时默认为 `null`
  - 基于 Plugin Registry、Run 模型/MCP 装配、Middleware capability/policy、执行动作生成静态策略评估
  - Meta-Agent 创建 Agent 和生图 Agent dry-run/运行都写入 policy evaluation
  - Workbench selectedRun、/api/workbench、/api/runs/:id 和 CLI `run show` 暴露 policy
  - Workbench Context tab 展示策略总览、检查项、权限和关联能力
  - 同步核心概念、运行时和 UI 文档
- 非本次范围：
  - 不实现真实阻断、审批弹窗、配额扣减或用户确认流
  - 不启动真实 MCP Server，不执行 MCP call
  - 不实现插件安装/卸载或远程市场
  - 不改变现有 Agent manifest schema
- 验收标准：
  - 新生成 trace 包含 `policy`，旧 trace 读取时默认为 `null`
  - Meta-Agent run 和 Image Agent dry-run 都能在 selectedRun 中看到 policy evaluation
  - Context tab 能看到 allowed / review_required / blocked / unknown 状态和风险摘要
  - `/api/runs/:id` 返回 trace 中的 policy
  - CLI `run show <run_id>` 打印 policy 摘要
- 测试 / 验证：
  - `npm test`：30 个测试全部通过
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(runtime): 增加 Policy Gate 快照`

## F-083 Async Worker + Trace Events v1

- 状态：已完成
- 所属节点：P1.10 Async Worker / P1.11 Observability
- 建议 scope：`runtime`, `agent`, `meta`, `workbench`, `docs`
- 背景：当前 Plan、Middleware 和 Policy 已经进入 Trace，但长任务仍表现为同步执行，缺少可入队的 Worker 任务对象和可回放事件流。后续取消、重试、流式 UI、恢复执行和 SubAgent handoff 都需要先有稳定的事件底座。
- 目标：建立最小 Async Worker 与 TraceEvent 骨架，让现有 Agent run 和 Meta-Agent create 都能记录 Worker envelope、关键事件流，并在 CLI、API 和 Workbench 中可见。
- 本次范围：
  - 新增 WorkerJobRecord / TraceEventRecord 类型和 RuntimeTrace.worker / RuntimeTrace.events
  - RuntimeStore 支持记录 Worker 状态迁移和 TraceEvent
  - 用 inline worker envelope 包裹 Meta-Agent 创建 Agent 和生图 Agent 运行
  - Step、Artifact、Policy、Trace 写入等关键节点自动记录事件
  - Workbench selectedRun、/api/workbench、/api/runs/:id 和 CLI `run show` 暴露 worker/events
  - Workbench Context tab 展示 Worker 任务快照和事件流
  - 同步核心概念、运行时、UI 文档和任务清单
- 非本次范围：
  - 不实现跨进程后台守护、持久化队列或并发调度
  - 不实现真实取消、重试、暂停/恢复或用户审批 UI
  - 不改变现有 Agent manifest schema
  - 不执行真实 MCP call
- 验收标准：
  - 新生成 trace 包含 `worker` 和 `events`，旧 trace 读取时 `worker: null`、`events: []`
  - Meta-Agent run 和 Image Agent dry-run 都能在 selectedRun 中看到 worker/events
  - Context tab 能看到 Worker state、queue、attempt 和 TraceEvent timeline
  - `/api/runs/:id` 返回 trace 中的 worker/events
  - CLI `run show <run_id>` 打印 worker 与 events 摘要
- 测试 / 验证：
  - `node --import tsx --test src/runtime/step-runner.test.ts`：通过
  - `node --import tsx --test src/runtime/workbench-data.test.ts`：通过
  - `node --import tsx --test src/ui/server.test.ts`：通过（需要本地监听 127.0.0.1）
  - `npm test`：通过，31 tests
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
  - 手动验收：`npm run dev -- agent run image-gen/prototype-v1 --prompt "worker events acceptance dashboard" --count 1 --size 1024x1024 --raw-prompt --dry-run`
  - CLI 验收：`npm run dev -- run show run-image-gen__prototype-v1-mq6u1wno-7dqtdb` 可见 `worker:` 和 `events:`
- 闭环 commit：`feat(runtime): 增加 Async Worker 事件骨架`

## F-084 Runtime Execution Modes v1

- 状态：已完成
- 所属节点：P1.3 Execution Runtime / P1.10 Async Worker / P1.11 Observability
- 建议 scope：`runtime`, `agent`, `meta`, `workbench`, `docs`
- 背景：Run 目前已有 `dryRun`、Worker queue、Policy 和 TraceEvent，但“本次到底以什么执行模式跑、为何跳过外部调用、由哪个入口调度、当前能力边界是什么”仍分散在多个字段中。后续取消、重试、恢复、审批和 replay 都需要一个稳定的执行模式快照。
- 目标：建立最小 Runtime Execution Modes v1，让 Meta-Agent 创建 Agent 和生图 Agent 运行都把执行模式写入 Trace，并在 CLI、API 和 Workbench Context tab 中可见。
- 本次范围：
  - 新增 `ExecutionModeRecord` / `ExecutionCapabilityRecord` 类型和 `RuntimeTrace.execution`
  - `RuntimeStore` 支持写入 execution mode 快照，旧 trace 读取时默认为 `null`
  - 为 Meta-Agent 创建 Agent 和生图 Agent dry-run/live/fallback 生成静态 execution mode
  - Workbench `selectedRun`、`/api/workbench`、`/api/runs/:id` 和 CLI `run show` 暴露 execution
  - Workbench 右侧 Context tab 展示执行模式、入口、队列、能力边界和限制
  - 同步核心概念、运行时、UI 文档和任务清单
- 非本次范围：
  - 不实现真实后台队列、跨进程 worker、取消、重试、暂停/恢复或 replay 执行
  - 不改变现有 `run.dryRun` 兼容字段
  - 不启动真实 MCP Server，不执行 MCP call
  - 不引入用户可编辑的执行模式选择 UI
- 验收标准：
  - 新生成 trace 包含 `execution`，旧 trace 读取时 `execution: null`
  - Meta-Agent run 和 Image Agent dry-run 都能在 selectedRun 中看到 execution mode
  - Image Agent 缺 Provider 配置时能把 effective mode 记录为 `dry_run` fallback
  - Context tab 能看到 execution mode、dispatch/queue、capability states 和 constraints
  - CLI `run show <run_id>` 打印 execution 摘要
- 测试 / 验证：
  - `node --import tsx --test src/runtime/workbench-data.test.ts`
  - `node --import tsx --test src/runtime/step-runner.test.ts`
  - `node --import tsx --test src/ui/server.test.ts`
  - `npm test`
  - `npm run typecheck`
  - `npm run build`
  - `git diff --check`
  - 手动验收：`npm run dev -- agent run image-gen/prototype-v1 --prompt "execution mode acceptance dashboard" --count 1 --size 1024x1024 --raw-prompt --dry-run`
  - CLI 验收：`npm run dev -- run show run-image-gen__prototype-v1-mq7haphz-1kulim` 可见 `execution:`、`execution_mode_selected` 和 capability states
  - 浏览器验收：`http://127.0.0.1:4179/ui/workbench/` Context tab 可见 `Agent 运行执行模式`、`Dry run`、`agent.run.inline`、`Image Provider call 跳过` 和 `MCP assembly 跳过`
- 闭环 commit：`feat(runtime): 增加 Execution Mode 快照`

## F-085 Sandbox Filesystem v1

- 状态：已完成
- 所属节点：P1.4 Sandbox Filesystem / P1.5 Artifact Service / P1.11 Observability
- 建议 scope：`runtime`, `agent`, `meta`, `workbench`, `docs`
- 背景：Artifact Preview 已能按路径粗略分类，但每次 Run 实际可用的 workspace、uploads、outputs、temp 和 traces 边界还没有进入 Trace。后续文件上传、PDF/DOCX 解析、恢复执行、权限审批和真实 worker sandbox 都需要稳定的沙盒文件系统快照。
- 目标：建立最小 Sandbox Filesystem v1，让 Meta-Agent 创建 Agent 和生图 Agent 运行都记录本次 Run 的沙盒目录、路径边界、写入策略和当前限制，并在 CLI、API 和 Workbench Context tab 中可见。
- 本次范围：
  - 新增 `SandboxFilesystemRecord` / `SandboxDirectoryRecord` 类型和 `RuntimeTrace.sandbox`
  - Runtime 入口创建并记录 `workspace`、`uploads`、`outputs`、`temp`、`traces` 目录快照
  - `RuntimeStore` 支持写入 sandbox 快照，旧 trace 读取时默认为 `null`
  - Workbench `selectedRun`、`/api/workbench`、`/api/runs/:id` 和 CLI `run show` 暴露 sandbox
  - Workbench 右侧 Context tab 展示沙盒目录、scope、writable 和 cleanup policy
  - 同步核心概念、运行时、UI 文档和任务清单
- 非本次范围：
  - 不实现真正进程隔离、chroot、Docker 或 worker sandbox
  - 不实现上传文件 UI、文件删除、下载权限或路径审批
  - 不迁移既有 Artifact 输出目录
  - 不执行真实 PDF/DOCX 解析或 MCP 文件访问
- 验收标准：
  - 新生成 trace 包含 `sandbox`，旧 trace 读取时 `sandbox: null`
  - Meta-Agent run 和 Image Agent dry-run 都能在 selectedRun 中看到 sandbox directories
  - `outputs` 指向本次实际产物根目录，`traces` 指向本次 trace 目录
  - Context tab 能看到 sandbox scope、directory kind、writable 和 cleanup policy
  - CLI `run show <run_id>` 打印 sandbox 摘要
- 测试 / 验证：
  - `node --import tsx --test src/runtime/step-runner.test.ts`：通过
  - `node --import tsx --test src/runtime/workbench-data.test.ts`：通过
  - `node --import tsx --test src/ui/server.test.ts`：通过（需要本地监听 127.0.0.1）
  - `npm test`：通过，33 tests
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
  - 手动验收：`npm run dev -- agent run image-gen/prototype-v1 --prompt "sandbox filesystem acceptance dashboard" --count 1 --size 1024x1024 --raw-prompt --dry-run`
  - CLI 验收：`npm run dev -- run show run-image-gen__prototype-v1-mq7j5zxh-9wvk2b` 可见 `sandbox:`、`outputs writable=yes cleanup=keep`、`traces writable=yes cleanup=keep` 和 `sandbox_created`
  - 浏览器验收：`http://127.0.0.1:4180/ui/workbench/` Context tab 可见 `沙盒文件系统`、`产物区`、`Trace 区`、`可写` 和 `清理策略`
- 闭环 commit：`feat(runtime): 增加 Sandbox Filesystem 快照`

## F-086 Artifact Delivery Manifest v1

- 状态：已完成
- 所属节点：P1.5 Artifact Service / P1.11 Observability
- 建议 scope：`runtime`, `agent`, `meta`, `workbench`, `docs`
- 背景：Artifact 已可登记和预览，Sandbox Filesystem 也记录了运行文件边界，但每次 Run “最终交付了哪些产物、哪个是主产物、总大小多少、当前交付能力限制是什么”仍分散在 artifacts 列表和 UI 细节中。后续多产物交付、下载、版本、diff 和采纳都需要一个稳定交付清单。
- 目标：建立最小 Artifact Delivery Manifest v1，让 Run Trace、CLI、API 和 Workbench 都能看到本次交付摘要。
- 本次范围：
  - 新增 `ArtifactDeliveryRecord` / `ArtifactDeliveryItemRecord` 类型和 `RuntimeTrace.delivery`
  - 基于当前 `ArtifactRecord` 和 Previewer v1 元数据生成静态 delivery manifest
  - `RuntimeStore` 支持写入 delivery，并记录交付清单事件
  - Meta-Agent 创建 Agent 和生图 Agent dry-run/live/failed 收口都写入 delivery
  - Workbench `selectedRun`、`/api/workbench`、`/api/runs/:id` 和 CLI `run show` 暴露 delivery
  - Workbench Artifact 面板展示交付摘要、主产物、总大小和当前限制
  - 同步核心概念、运行时和 UI 文档
- 非本次范围：
  - 不实现 zip 打包下载、真实下载权限审批或删除 UI
  - 不实现 Artifact 版本图、diff 编辑器或采纳工作流
  - 不改变 Previewer v1 的 PDF/DOCX 解析能力
  - 不引入数据库或远程 Artifact Service
- 验收标准：
  - 新生成 trace 包含 `delivery`，旧 trace 读取时 `delivery: null`
  - Meta-Agent run 的 delivery 能列出草案文件并标记 primary artifact
  - Image Agent dry-run 的 delivery 能明确显示 0 个可交付产物且状态为 `empty`
  - Workbench Artifact 面板可见交付清单摘要
  - CLI `run show <run_id>` 打印 delivery 摘要
- 测试 / 验证：
  - `node --import tsx --test src/runtime/artifacts.test.ts src/runtime/workbench-data.test.ts`：通过
  - `node --import tsx --test src/ui/server.test.ts`：通过（本地监听 127.0.0.1）
  - `npm test`：通过，33 tests
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
  - 手动验收：`npm run dev -- meta create-agent --prompt "Create a delivery manifest acceptance Agent that stores traceable artifact files" --name "Delivery Manifest Acceptance Agent" --description "A local draft used to verify Artifact Delivery Manifest in Workbench."`
  - CLI 验收：`npm run dev -- run show meta-create-custom__delivery-manifest-acceptance-agent-v1-mq7kfbc7-uq1v28` 可见 `delivery:`、`Meta-Agent 草案交付清单 ready artifacts=12` 和 `artifact_delivery_prepared`
  - 浏览器验收：`http://127.0.0.1:4181/ui/workbench/` Artifact tab 可见 `交付清单`、`可交付`、`产物 12`、`总大小 6.2KB` 和 `可打开 12`
- 闭环 commit：`feat(runtime): 增加 Artifact Delivery Manifest`

## F-087 Artifact Download API v1

- 状态：已完成
- 所属节点：P1.5 Artifact Service / P1.11 Observability
- 建议 scope：`runtime`, `workbench`, `docs`
- 背景：Artifact Delivery Manifest 已经能告诉用户本次 Run 交付了哪些产物，但 Artifact Service 仍只支持预览、文本读取和本机系统打开。后续交付闭环需要先具备最小的“已登记产物可下载”能力。
- 目标：增加单个 Artifact 下载 API，并在 Workbench Artifact 面板提供下载入口。
- 本次范围：
  - 新增 `GET /api/artifacts/:id/download`，按已登记 Artifact ID 返回文件流
  - 下载响应包含 `Content-Type`、`Content-Length`、`Content-Disposition` 和 Artifact ID header
  - 仅允许下载 Previewer 判定为 workspace/artifacts/traces/agents/temp 范围内的本地文件；external 路径不开放下载
  - Workbench Artifact 面板新增“下载”按钮，指向单个产物下载接口
  - 同步运行时、UI 原型和 Workbench README 文档
- 非本次范围：
  - 不实现 zip 打包下载、批量导出、签名 URL、下载审计或权限审批 UI
  - 不实现 Artifact 删除、版本图、diff 编辑器或采纳工作流
  - 不启动真实 MCP Server，不执行 MCP call
- 验收标准：
  - `GET /api/artifacts/:id/download` 能下载 Meta-Agent 生成的 `manifest.yaml`
  - Office-like 二进制 Artifact 也能以 metadata MIME 和原始字节下载
  - external 路径 Artifact 不会被直接下载
  - Workbench Artifact 面板显示下载按钮，并在无选中 Artifact 时禁用
  - 旧 `/api/artifacts/:id`、`/api/artifact-preview`、`/api/artifact-content` 兼容不变
- 测试 / 验证：
  - `node --import tsx --test src/ui/server.test.ts`：通过，5 tests
  - `npm test`：通过，33 tests
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
  - 手动验收：`npm run dev -- meta create-agent --prompt "Create an artifact download acceptance Agent that stores a manifest file for download verification" --name "Artifact Download Acceptance Agent" --description "A local draft used to verify Artifact Download API in Workbench."`
  - API 验收：`GET /api/artifacts/art-meta-create-custom__artifact-download-acceptance-agent-v1-mq7o2q59-e3gvb0-1/download` 返回 `200`、`text/yaml; charset=utf-8`、`attachment; filename="manifest.yaml"` 和 `X-Moyu-Artifact-Id`
  - 浏览器验收：`http://127.0.0.1:4182/ui/workbench/` Artifact tab 可见 `交付清单`、真实草案产物列表和可用的 `下载` 按钮
- 闭环 commit：`feat(runtime): 增加 Artifact 下载接口`

## F-088 Meta-Agent LLM Provider v1

- 状态：已完成
- 所属节点：P2.2 需求澄清对话 / P2.3 Agent 规划器 / P6.3 配置中心
- 建议 scope：`meta`, `config`, `docs`, `test`
- 背景：README 已说明 Meta-Agent 可以生成 Agent 草案，但工程状态仍明确它是规则化脚手架生成器。当前用户目标是“能真的配置一个大模型地址，然后使用 meta agent 创建出一个 agent”。
- 目标：让 `meta create-agent` 在配置 OpenAI-compatible 对话模型后，优先调用真实 LLM 生成 Agent 规格，再沿用现有本地草案写入、校验、Trace、Workbench 和安装流程。
- 本次范围：
  - 新增 `MOYU_LLM_PROVIDER_BASE_URL` / `MOYU_LLM_PROVIDER_API_KEY` / `MOYU_LLM_PROVIDER_MODEL` 环境配置
  - 新增 OpenAI-compatible `chat/completions` 客户端
  - Meta-Agent 创建草案时优先使用 LLM 生成 `agent_id`、名称、描述、类型和标签
  - LLM 不可用或返回异常时回退到现有本地规则，并在 Trace 中记录兜底原因
  - 生成的普通任务 Agent 使用 `conversation-primary` routing，图片 Agent 使用 `image-generation` routing
  - 同步 README、工程状态和环境变量示例
- 非本次范围：
  - 不实现多轮需求澄清、计划确认或自动追问
  - 不让 LLM 直接写任意文件内容
  - 不实现 Provider 健康检查 UI 或 API key 保存 UI
  - 不替换现有 Agent validator / install review 流程
- 验收标准：
  - 配置 LLM provider 后，`meta create-agent` 会调用 `/v1/chat/completions`
  - LLM 返回的合法 `agent_id` 可落成 reviewable draft
  - Trace 的 `spec-draft` step 可见 `source: llm`、模型和 provider
  - Provider 失败时仍能生成本地规则草案，Trace 可见 fallback reason
  - 未配置 LLM provider 时旧 dry-run / Workbench 流程不回退
- 测试 / 验证：
  - `node --import tsx --test src/meta/create-agent.test.ts`：通过，2 tests
  - `node --import tsx --test src/runtime/workbench-data.test.ts`：通过，3 tests
  - `node --import tsx --test src/ui/server.test.ts`：通过，5 tests（需要本地监听 127.0.0.1）
  - `npm test`：通过，35 tests（需要本地监听 127.0.0.1）
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
- 闭环 commit：`feat(meta): 接入 Meta-Agent LLM 规格草案`

## F-089 Meta-Agent 对话式创建 v1

- 状态：已完成
- 所属节点：P2.2 需求澄清对话 / P2.3 Agent 规划器 / P3.1 Agent Chat Session
- 建议 scope：`meta`, `runtime`, `workbench`, `test`, `docs`
- 背景：F-088 已经支持配置大模型参与 Agent 规格生成，但用户真正需要的是通过多轮对话创建 Agent，而不是一次性提交 prompt。
- 目标：建立最小对话式 Meta-Agent 创建闭环：用户在 Workbench 对话框里描述需求，Meta-Agent 追问或整理确认，用户确认后生成可审核 Agent 草案。
- 本次范围：
  - 新增 Meta-Agent 对话会话 API
  - 对话消息写入 WorkStore，并在 Workbench 中作为真实消息展示
  - 支持“需求不足时追问、需求足够时请求确认、确认后创建 Agent 草案”的最小状态机
  - 确认创建后复用 `createAgentWithMeta`、Run Trace、Artifact、Draft 和安装审核流程
  - 生成 Run 绑定到同一个 Work，支持左侧任务继续选择和恢复
  - Workbench composer 在选中 Meta-Agent 时调用真实对话 API，静态预览保持演示 fallback
- 非本次范围：
  - 不实现复杂多轮 LLM 澄清策略
  - 不实现用户可编辑结构化表单
  - 不实现流式 token 输出或后台队列
  - 不让模型直接绕过本地 validator 写文件
- 验收标准：
  - 用户第一轮描述需求后，Workbench 能持久化 user / agent 对话消息
  - 需求足够但未确认时，Meta-Agent 回复确认 checkpoint，不创建文件
  - 用户回复“确认创建”后，生成 Agent 草案和 Trace
  - 生成 run 与前面的对话消息处于同一个 Work
  - 静态预览和非 Meta-Agent 发送行为不回退
- 测试 / 验证：
  - `node --import tsx --test src/meta/conversation.test.ts src/meta/create-agent.test.ts`：通过，3 tests
  - `node --import tsx --test src/ui/server.test.ts`：通过，6 tests（需要本地监听 127.0.0.1）
  - `npm test`：通过，37 tests（需要本地监听 127.0.0.1）
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `git diff --check`：通过
  - 浏览器验收：`http://127.0.0.1:4180/ui/workbench/` 可渲染，元智能体入口可见，composer 可输入，发送按钮可用
- 闭环 commit：`feat(meta): 增加对话式 Agent 创建`

## F-090 Meta-Agent 真实 LLM 对话创建 v1

- 状态：已完成
- 所属节点：P2.2 需求澄清对话 / P2.3 Agent 规划器 / P3.1 Agent Chat Session
- 建议 scope：`meta`, `config`, `workbench`, `test`, `docs`
- 背景：F-089 建立了对话式创建入口，但对话决策仍主要依赖本地规则状态机，确认创建时的 Agent 规格生成也允许静默回退到本地规则。当前目标是“通过真实可配置的大模型对话生成 Agent”，不能用规则模拟成功。
- 目标：让 Workbench Meta-Agent 对话链路真实调用已配置的 OpenAI-compatible 对话模型完成澄清、确认和创建决策；确认创建时强制使用 LLM 生成 Agent 规格，缺少配置或 provider 失败必须明确报错。
- 本次范围：
  - Meta-Agent 对话每轮调用 `MOYU_LLM_PROVIDER_*` 配置的大模型返回结构化决策和回复
  - 缺少 LLM 配置时 `/api/meta/conversation` 返回明确错误，不生成规则化假回复
  - Provider 调用失败、返回非 JSON 或决策字段非法时明确失败，不静默 fallback
  - 用户确认创建后，`createAgentWithMeta` 在对话链路强制 LLM 规格草案，禁止回退到本地规则
  - 保留 CLI `meta create-agent` 既有 fallback 行为，避免扩大本次改动面
  - 同步 README、工程状态和 Workbench 文档
- 非本次范围：
  - 不实现流式 token 输出
  - 不让 LLM 直接写任意文件内容，仍走本地 scaffold、validator、trace 和安装审核
  - 不新增 API key 保存 UI 或 provider 健康检查 UI
  - 不重构 Workbench 大文件
- 验收标准：
  - 未配置 `MOYU_LLM_PROVIDER_BASE_URL` / `MOYU_LLM_PROVIDER_API_KEY` 时，对话创建失败并提示需要配置 LLM
  - 配置 LLM 后，用户第一轮对话会调用 `/v1/chat/completions`，回复内容来自 LLM 结构化决策
  - LLM 判断可创建并收到用户确认后，系统再次通过 LLM 生成 Agent spec，生成草案和 Trace
  - 对话链路 provider 失败或 spec 生成失败时不会生成本地规则 Agent
  - 测试覆盖缺配置、provider 失败、对话决策、确认创建和 spec 强制 LLM
- 测试 / 验证：
  - `node --import tsx --test src/meta/conversation.test.ts src/meta/create-agent.test.ts src/ui/server.test.ts`：Meta / create-agent 单元通过；server 子测试在 sandbox 内因监听 `127.0.0.1` 被 EPERM 阻止
  - `node --import tsx --test src/ui/server.test.ts`：通过，7 tests（提升权限绑定本地临时端口）
  - `npm run typecheck`：通过
  - `npm run build`：通过
  - `npm test`：通过，41 tests（提升权限绑定本地临时端口）
  - `git diff --check`：通过
- 闭环 commit：`feat(meta): 强制对话式创建走真实 LLM`
